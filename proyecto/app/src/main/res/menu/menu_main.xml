<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/nav_limpiar"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:icon="@drawable/ic_action_clear" />

    <item
        android:id="@+id/nav_eliminar"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:icon="@drawable/ic_action_trash" />

    <item
        android:id="@+id/nav_map"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:icon="@drawable/ic_map" />

    <item
        android:id="@+id/nav_filters"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:icon="@drawable/ic_search" />

    <item
        android:id="@+id/nav_deshacer_filtro"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:icon="@drawable/ic_deshacer_filtro" />
</menu>
